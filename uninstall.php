<?php
/**
 * Uninstall handler for ACZ Elements.
 *
 * This file intentionally leaves saved settings in place so reinstalling the
 * plugin does not remove global template/theme configuration.
 */

if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
    exit;
}
