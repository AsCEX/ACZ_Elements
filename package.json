{
  "name": "acz-elements",
  "version": "1.2.9",
  "private": true,
  "description": "Build tooling for ACZ Elements for Elementor release assets.",
  "scripts": {
    "build": "npm run build:vendor",
    "build:vendor": "node scripts/build-vendor.mjs",
    "version:bump": "node scripts/bump-version.mjs",
    "release:tag": "node scripts/release-tag.mjs",
    "release:check": "npm run build:vendor && find . -path './.git' -prune -o -name '*.php' -print0 | xargs -0 -n 1 php -l"
  },
  "dependencies": {},
  "devDependencies": {
    "swiper": "12.1.3",
    "coloured-icons": "https://github.com/dheereshag/coloured-icons/archive/refs/heads/master.tar.gz"
  }
}
